{
  "name": "@w7s-io/example-serverless-database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild backend/src/index.ts --bundle --format=esm --platform=neutral --target=es2022 --outfile=backend/index.js",
    "check": "tsc --noEmit && npm run build",
    "db:generate": "drizzle-kit generate"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260524.0",
    "drizzle-kit": "^0.31.9",
    "esbuild": "^0.27.0",
    "typescript": "5.9.3"
  }
}
